<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{personalInfo.name}}的简历</title>
    <style>
        body { font-family: Arial, sans-serif; line-height: 1.6; margin: 20px; }
        .container { max-width: 800px; margin: auto; padding: 20px; border: 1px solid #ddd; }
        h1, h2 { color: #333; }
        h1 { text-align: center; }
        .section { margin-bottom: 20px; }
        .section h2 { border-bottom: 1px solid #eee; padding-bottom: 5px; }
        .job, .education-entry { margin-bottom: 10px; }
        .job p, .education-entry p { margin: 5px 0; }
        ul { padding-left: 20px; }
        li { margin-bottom: 5px; }
    </style>
</head>
<body>
    <div class="container">
        <h1>{{personalInfo.name}}</h1>
        <p style="text-align: center;">{{personalInfo.email}} | {{personalInfo.phone}}</p>

        <div class="section">
            <h2>教育背景</h2>
            {{#each education}}
            <div class="education-entry">
                <p><strong>{{school}}</strong> - {{degree}} in {{major}}</p>
                <p>{{startDate}} - {{endDate}}</p>
            </div>
            {{/each}}
        </div>

        <div class="section">
            <h2>工作经历</h2>
            {{#each experience}}
            <div class="job">
                <p><strong>{{company}}</strong> - {{position}}</p>
                <p>{{startDate}} - {{endDate}}</p>
                <ul>
                    {{#each responsibilities}}
                    <li>{{this}}</li>
                    {{/each}}
                </ul>
            </div>
            {{/each}}
        </div>

        <div class="section">
            <h2>技能</h2>
            <ul>
                {{#each skills}}
                <li>{{this}}</li>
                {{/each}}
            </ul>
        </div>
    </div>
</body>
</html>